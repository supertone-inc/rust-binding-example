{
  "name": "hello-neon",
  "main": "hello",
  "scripts": {
    "build": "cargo-cp-artifact -nc hello/index.node -- cargo build --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "cargo test"
  },
  "devDependencies": {
    "cargo-cp-artifact": "^0.1"
  }
}
