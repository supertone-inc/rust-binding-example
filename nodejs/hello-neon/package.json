{
  "name": "hello",
  "main": "hello",
  "scripts": {
    "build": "cargo-cp-artifact -ac hello-nodejs-neon hello/index.node -- cargo build --message-format=json-render-diagnostics",
    "install": "npm run build",
    "test": "jest"
  },
  "devDependencies": {
    "cargo-cp-artifact": "^0.1",
    "jest": "^27.0.6"
  },
  "engines": {
    "node": ">= 14"
  }
}
