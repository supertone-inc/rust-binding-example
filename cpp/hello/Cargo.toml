[package]
name = "hello_cpp"
version = "0.1.0"
authors = []
edition = "2018"

[lib]
name = "hello"
crate-type = ["cdylib"]

[dependencies]
hello = {path = "../../rust/hello"}
libc = "0.2"

[build-dependencies]
cbindgen = "0.17"
